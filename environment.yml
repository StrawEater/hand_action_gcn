name: gcn
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch==2.5.1
  - torchvision==0.20.1
  - pytorch-cuda=12.1
  - numpy==2.2.6
  - pyyaml
  - tqdm
  - pillow

# After creating the environment, build the CUDA shift extension:
#   cd model/Temporal_shift/cuda
#   pip install -e .
